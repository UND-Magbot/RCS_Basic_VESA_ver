# 현장 시연 세팅 체크리스트 (S300 업체 방문 / 30망)

> 상황: 30.1 공유기(iptime5G, 게이트웨이 192.168.30.1)를 들고 가서
> 서버노트북 1대 + 현장 로봇 1대 + 태블릿 1대로 시연.
> **이번 목표**: 현장 맵핑 → C1 + J1~J3 등록 → 동기화 → 좌표 확인 → 태블릿(J1)·노트북(J2·J3)에서 **렉 없이 호출** 반복.
> **현장은 인터넷 안 됨 → 이 파일 보고 혼자 진행.**
> 최종 수정: **2026-08-10** (8/10 실기 테스트 반영 — 7-1·7-2·7-3·6-1·8번 갱신)
>
> **8/10 주요 변경**
> - 7-2: 동기화 판정을 `create_time` → **`last_modified_time`** 으로 (기존 지침대로 하면 오판합니다)
> - 7-3: "활성 맵이 섞이면 엉뚱한 좌표로 간다"는 **부정확** — 실제 위험은 `charging_id`/`standby_id`
> - 7-1·8: 맵을 저장하면 `charging_id` 가 **NULL로 날아간다**(FK `ON DELETE SET NULL`)

---

## 0. 네트워크 구조 (한 장 그림)

```
태블릿 ──(:8002)──▶ 서버노트북(백엔드) ──(:8090)──▶ 로봇(192.168.30.100)
                        │
관제(브라우저)─(:3000)──┤  ← 노트북에서 보면 localhost
                        └── DB(같은 노트북, 127.0.0.1, rcs_vesa_db)
```
전부 `192.168.30.x` 같은 망. 로봇=.100, 노트북=.xx(공유기가 줌), 태블릿=.xx.

---

## 준비물 체크 (출발 전)

- [ ] iptime5G 공유기 + 전원어댑터 + 랜케이블
- [ ] 서버노트북 (충전기 포함) — **백엔드/프론트 사전 구동 테스트 완료**
- [ ] 태블릿 + 충전기 + **TabletApp APK**(현장에서 인터넷 없으니 미리 설치)
- [ ] 로봇 충전독
- [ ] 로봇 핫스팟 비번 `12345678`, rb-admin 로그인 `guest@autoxing.com`

---

## 1. 로봇 IP 세팅 (30.100)

1. 로봇 전원버튼 **9~10회 연타** → 로봇 핫스팟(설정모드) 켜짐
2. PC wifi를 **로봇 핫스팟**에 연결 (비번 `12345678`)
3. 브라우저 → `http://192.168.12.1:8000/wifi_setup/`
4. 설정:
   - Route Mode Setup: `wlan0_first` → **먼저 submit**
   - WIFI Setup: `station(Advanced)`, wifi=`iptime5G` (+ wifi 비번)
   - 고정IP JSON:
     ```json
     {"ipv4.method":"manual","ipv4.address":"192.168.30.100/24",
      "ipv4.gateway":"192.168.30.1","ipv4.dns":"192.168.30.1"}
     ```
   - **그다음 WIFI Setup submit** (순서 지킬 것)
5. PC wifi를 `iptime5G` 다시 연결
6. 확인: 브라우저 `http://192.168.30.100:8090/rb-admin` 열리면 성공

⚠️ 세 번째 자리(30) 절대 바꾸지 말 것. 끝자리(호스트)만. 공유기 로그인 불필요.

---

## 2. 서버노트북 실행 (⚠️ 로컬 DB로!)

- 백엔드: `BackEnd` 폴더에서 **`run_localdb.ps1`** 실행
  (= DB 127.0.0.1, 포트 8002. 공장 192.168.0.21 바라보는 `run_local.ps1` 쓰면 먹통!)
- 프론트: `frontend`에서 `npm run dev` (포트 3000)
- 노트북 30망 IP 확인 후 **메모**:
  ```powershell
  Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -eq 'Wi-Fi'}
  ```

### 2-1. 태블릿 접속용 방화벽 — **현재 상태는 정상. 건드리지 말 것**

8/7 확인 결과, 방화벽에 **python.exe 인바운드 허용 규칙이 이미 있습니다.**

```
규칙: python.exe / Allow / Inbound / Profile: Public
경로: C:\Users\ASUS\AppData\Local\Programs\Python\Python311\python.exe
```

`run_localdb.ps1`은 uvicorn을 이 python으로 띄우므로(8002 리스닝 프로세스 = 위 경로 확인),
**같은 방식으로 실행하는 한 현장에서도 그대로 열립니다.** 지금 공장에서 태블릿이 잘 되는 이유가 이것입니다.

> ❌ **`Set-NetConnectionProfile ... -NetworkCategory Private` 을 실행하지 마세요.**
> 위 규칙은 **Public 프로필 전용**이라, 네트워크를 Private으로 바꾸면 오히려 규칙이 적용 안 되어 **막힙니다.**

**현장에서 안 될 수 있는 경우는 두 가지뿐입니다:**

| 경우 | 증상 | 대처 |
|---|---|---|
| 백엔드를 **다른 python**(가상환경 등)으로 실행 | 최초 실행 시 Windows 방화벽 팝업 | 팝업에서 **"액세스 허용"** 클릭 |
| 현장 Wi-Fi가 **Private**으로 잡힘 | 태블릿 연결 안 됨 | 아래 보험 규칙 추가 |

**보험 (선택) — 출발 전에 미리 해두면 어떤 상황에서도 열립니다.** 기존 설정을 바꾸지 않고 규칙만 추가하는 것이라 안전합니다. 관리자 PowerShell:
```powershell
New-NetFirewallRule -DisplayName "RCS Backend 8002" -Direction Inbound -Protocol TCP -LocalPort 8002 -Action Allow -Profile Any
```

- [ ] **확인(이것만 하면 됨)**: 태블릿 브라우저에서 `http://192.168.30.xx:8002/ping` 열어서 응답 오는지

---

## 3. DB에서 로봇 IP 맞추기

- `rcs_vesa_db.robots` 테이블:
  - 호출할 로봇 1개 행: `ip_address = 192.168.30.100`, `is_active = 1`
  - 나머지 로봇 행: **`is_active = 0`** (반드시. 안 그러면 배차가 오프라인 로봇을 붙잡음)

---

## 4. 주소 정리표

| 항목 | 값 |
|---|---|
| DB | 127.0.0.1 (run_localdb.ps1) |
| 로봇 IP (DB robots.ip_address) | 192.168.30.100 |
| rb-admin | http://192.168.30.100:8090/rb-admin |
| 관제시스템(노트북에서) | http://localhost:3000 |
| 백엔드 API(노트북에서) | http://localhost:8002 |
| 태블릿 앱 서버주소 | http://192.168.30.xx:8002 (**노트북 IP**) |
| 태블릿 앱 기본값(잘못됨) | ~~http://192.168.0.21:8002~~ ← 반드시 바꿀 것 |

---

## 5. 맵핑

### ⚠️ 5-0. 맵핑 시작 전 필수 확인 (8/7 실측 — 이거 안 보면 무한 대기)

- [ ] **로봇을 충전기에서 분리** — 도킹된 채로 시작하면 로봇이 요청은 받아주고 `state: running`까지 만들지만 **맵을 하나도 안 만듭니다.** 관제 화면은 "맵 데이터 수신 대기 중"에서 영원히 안 넘어갑니다. 에러도 안 뜹니다.
- [ ] **잭이 내려가 있는지** — `progress 0.0`이어야 함 (`state:"hold"`는 위/아래와 무관하니 보지 말 것)
- [ ] **로봇 경보 없는지** — rb-admin에서 알람 확인. `9019 Bottom sensor heartbeat error`가 있으면 콜드 부팅(전원 차단 후 재기동)

### 5-1. 맵핑 진행

1. rb-admin(또는 관제 맵핑모달)에서 로봇 수동주행하며 맵핑
2. **⚠️ 시작 후 30초 안에 맵이 그려지기 시작하는지 확인.** 안 그려지면 즉시 중지하고 5-0부터 다시. (계속 기다려도 절대 안 나옴)
3. 시연 동선(C1 ↔ J1 ↔ J2 ↔ J3) 전체를 **천천히** 주행. 통로는 왕복해서 양쪽 벽이 다 찍히게
4. 맵핑 **중지 → 저장**

---

## 6. POI 등록 (C1 + J1~J3)

**로봇을 실제 그 자리에 세우고 "현 위치" 버튼으로 찍는 게 가장 정확합니다.** 캔버스 클릭은 오차가 납니다.

| POI | 이름 | 유형 | 방법 |
|---|---|---|---|
| 충전소 | `C1` | 충전소 | 로봇을 **충전독에 도킹시킨 상태**에서 "충전소" 버튼 |
| **충전소 경유지** | **`C1-1`** | 작업 위치(경유지) | 아래 6-1 참조 — **반드시 등록** |
| 작업 위치 | `J1` `J2` `J3` | 작업 위치 | 로봇을 그 자리에 세우고 "작업 포인트" 버튼 |

### ⭐ 6-1. 충전소 경유지 `C1-1` — 도킹 실패(103) 예방

배차 코드는 충전소 복귀 시 **`{충전소명}-1` 이름의 POI를 먼저 경유**하도록 이미 만들어져 있습니다
(`dispatch_service.py:113-118`, `:271-294` — 1단계 사전 접근 → 2단계 charge 도킹).
**이 POI가 없으면 충전소 좌표로 곧장 돌진**하므로, 접근각이 나쁘면 `103 invalid_charge_dock`으로 실패합니다.

- 이름을 **정확히 `C1-1`** 로 (충전소 이름 + `-1`). 오타 나면 코드가 못 찾고 조용히 넘어갑니다
  (8/10 사례: 경유점을 `W1` 로 만들어뒀는데 **위치·방향은 완벽했지만** 이름이 달라 코드가 못 찾았습니다.
   이름만 `C1-1` 로 바꾸니 바로 동작 — `move 3628` 이 그 좌표로 나가는 것을 확인)
- ⚠️ **`charging_id` 가 비어 있으면 `C1-1` 을 만들어도 소용없습니다.** 경유지를 찾는 코드가
  충전소 복귀 블록 **안에** 있어서, `charging_id` 가 NULL이면 통째로 건너뜁니다(8번 항목 먼저 확인)
- 위치: **충전독 정면 축 위 약 1.0~1.5m**
  - 0.5m 미만 → 자세 고칠 여유가 없어 소용없음
  - 2.5m 초과 → 경유 의미가 옅어지고 시간만 늘어남
- 자세: 로봇을 그 자리에 세우고 **충전독을 바라보게** 한 뒤 "현 위치"로 등록 (방향이 중요)
- 참고: 자동 생성되는 `C1 Docking Point`는 0.3m 앞이라 **경유지로 쓰기엔 너무 가깝습니다.** 별개로 찍어야 합니다

### ⚠️ 확인 사항

- [ ] **각 POI 주변에 여유 공간 0.6m 이상** 확보 (8/7에 픽업 지점 여유가 0.46m라 렉 적재 시 경로 계산이 아예 실패한 사례 있음). 렉 없이 하면 0.4m로도 되지만, 나중에 렉 쓸 걸 생각하면 처음부터 여유 있게
- [ ] **J1~J3를 서로 충분히 떨어뜨릴 것** — 너무 가까우면 zone 점유로 대기가 걸림
- [ ] 렉 없이 시연하므로 **랙 위치(standby) POI는 등록 안 해도 됨**

---

## 7. 저장 → 동기화 → ⚠️ POI id 재확인

1. 관제에서 **저장**
2. **동기화(MAP-014)** — 대상 로봇 선택 + 덮어쓸 로봇 맵 선택
3. 모니터링 **기본 맵 적용(MAP-013)**

### ⚠️ 7-1. POI id는 저장할 때마다 바뀝니다 (미해결이슈 5번) — **가장 자주 당하는 함정**

맵을 저장하면 POI가 delete+재생성되어 **id가 전부 바뀝니다.** 그래서 순서가 중요합니다:

> **맵/POI를 완전히 확정한 다음에** → 저장 → **그 후에** 아래 8·9번(충전소 지정, 태블릿 세팅)을 합니다.
> 나중에 POI를 **이름 한 글자라도** 고쳐서 다시 저장하면 **8·9번을 전부 다시 해야 합니다.**

**저장하면 이 세 가지가 동시에 깨집니다 (8/10 실제 발생):**

| 깨지는 것 | 어떻게 되나 |
|---|---|
| `robots.charging_id` / `standby_id` | **NULL로 날아감** — DB 외래키가 `ON DELETE SET NULL` 이라 자동으로 지워집니다 |
| 태블릿 POI 주소 `/tablet/poi/{id}` | 그 id가 없어져서 "위치 정보를 찾을 수 없어요" 카드가 뜹니다 |
| `tablet_slots.poi_id` | 옛 POI를 가리켜 엉뚱한 곳으로 호출됩니다 |

> 8/10 사례: `W1` → `C1-1` 로 **이름만** 바꿔 저장했는데 map 26의 POI 6개 id가 전부 밀렸고
> (1057~1062 → 1063~1068), 방금 세팅한 `charging_id` 가 NULL이 되고 태블릿 화면도 먹통이 됐습니다.

- [ ] 저장 후 POI id 확인해서 메모:
  ```
  http://localhost:8002/api/map/active-pois
  ```
- [ ] **저장했으면 반드시**: 충전소 지정(8번) + 태블릿 주소/슬롯(9번)을 새 id로 다시 세팅

### 7-3. 활성 맵 정리 (**8/10 정정 — 급하지 않습니다**)

> ⚠️ **이전 문구 정정**: "활성 맵이 섞여 있어 코드가 id 큰 것 하나만 쓰므로 엉뚱한 맵의 `C1-1` 로 간다"는
> **부정확했습니다.** 코드는 **`area_id` 로 먼저 거르고** 그 안에서 id 최대를 씁니다
> (`dispatch_service._find_charge_approach_poi`, `routers/dispatch._list_available_pois`).
> area마다 활성 맵이 1개씩이므로, **로봇의 `area_id` 만 맞으면 활성 맵이 몇 개든 혼선이 없습니다.**
> 8/10에 활성 맵 7개 상태로 정상 동작하는 것을 확인했습니다.

**진짜 위험한 건 맵 개수가 아니라 `charging_id`/`standby_id` 입니다** — 이 둘은 area 필터 없이
**POI id로 직접** 읽기 때문에, 다른 맵의 POI를 가리키고 있으면 그 좌표로 그냥 갑니다.
(8/10 실측: 로봇 6이 area 26인데 `standby_id` 는 map 14의 R3 → map 26 기준 **7.58m 밖**)

- [ ] **로봇의 `area_id` 가 이번에 만든 맵의 area 인지** 확인 (이게 핵심)
- [ ] **`charging_id`/`standby_id` 가 그 맵의 POI 인지** 확인
- [ ] 안 쓰는 로봇은 **`is_active = 0`** (배차가 오프라인 로봇을 붙잡지 않도록)
- [ ] (선택) 옛 맵 비활성화 — 화면 정리 목적. **단 참조를 먼저 옮긴 뒤에** 할 것

### ⚠️ 7-2. 동기화가 실제로 갔는지 확인 (**8/10 수정 — 판정 기준이 바뀌었습니다**)

> ❌ **`create_time` 으로 판단하지 마세요.** 로봇의 기존 맵 슬롯에 **덮어쓰기**로 동기화하면
> `create_time` 은 그 슬롯이 **처음** 만들어진 시각 그대로 남습니다. 8/10에 이걸로
> "2월 옛날 맵을 쓰는 중"이라고 **오판**했는데, 실제로는 그날 만든 맵이 정상 적용돼 있었습니다.

- [ ] **`last_modified_time`** 이 방금 동기화한 시각인지 확인:
  ```
  http://192.168.30.100:8090/maps/{맵id}
  ```
  `last_modified_time` 이 갱신됐고 `map_version` 이 올라갔으면 적용된 것입니다.
  (`current-map` 응답에는 `last_modified_time` 이 없으므로 `/maps/{id}` 를 봐야 합니다)

- [ ] **확실하게 하려면** 둘 중 하나로 대조하세요:
  - `grid_origin_x/y` 가 DB의 그 맵과 같은지
  - 맵 이미지가 같은지 — 로봇 `maps/{id}.png` 와 DB `robot_maps.image_url` 파일의 **md5 비교**
    (8/10에 이 방법으로 확정: 둘 다 23500 bytes, md5 동일)

---

## 8. 충전소 지정 (작업 종료 → 복귀에 필요) — **꼭 확인하세요**

- [ ] 로봇 상세 정보(MON-003)에서 **충전소를 C1으로 지정** (`robots.charging_id`)
- [ ] 지정 안 하면 태블릿 "작업 종료" 시 **로봇이 그 자리에 그대로 서 있습니다.**
      에러도 안 뜨고 세션은 `completed` 로 끝나서 겉보기엔 정상처럼 보입니다 (8/10 실측)
- [ ] **판정법**: [작업 종료] 후 `chassis/moves` 에 **`creator: rcs` + `type: charge`** 가 생기면 정상입니다.
      `robot-admin-web`(수동)이나 `control_unit`(로봇 자체)만 보이면 백엔드가 명령을 안 보낸 것입니다
- [ ] ⚠️ **맵을 다시 저장하면 이 값이 NULL로 날아갑니다**(7-1 참조) — 저장했으면 여기부터 다시
- [ ] 렉 없이 시연이므로 **랙 위치(standby_id)는 비워둬도 됨**

---

## 9. 호출 화면 세팅 (태블릿 J1 / 노트북 J2·J3)

호출 페이지는 **두 가지 방식**이 있습니다. 슬롯 등록이 번거로우면 **POI 기반이 더 안전합니다.**

| 방식 | URL | 비고 |
|---|---|---|
| POI 기반 (**권장**) | `http://192.168.30.xx:8002/api/dispatch/tablet/poi/{POI_ID}` | 슬롯 등록 불필요. 7-1에서 메모한 id 사용 |
| 슬롯 기반 | `http://192.168.30.xx:8002/api/dispatch/tablet/{슬롯번호}` | 슬롯을 먼저 등록해야 함 |

- **태블릿(J1)**: 앱 초기 설정에 **서버주소** + **슬롯번호**를 넣습니다(앱은 슬롯 방식). 슬롯을 미리 등록해두세요:
  ```
  PUT http://localhost:8002/api/dispatch/slots/1   body: {"poi_id": <J1의 id>}
  ```
  확인: `http://localhost:8002/api/dispatch/slots`
- **노트북(J2·J3)**: 브라우저 탭 2개로 POI 기반 URL을 열어둡니다. 등록 절차 없이 바로 됩니다.

- [ ] 미등록 슬롯 번호를 넣으면 화면은 뜨지만 **POI가 0이라 호출이 동작하지 않습니다.** 화면 상단에 표시되는 위치명이 `J1`인지 `슬롯 1`인지로 구분하세요

---

## 10. 좌표 가는지 확인

- [ ] 관제 맵에서 **"로봇 연결"** 을 눌러야 마커가 실시간으로 움직입니다 (위치재조정 버튼과는 별개 기능)
- [ ] 로봇 위치추정이 정상인지 — rb-admin에서 로봇이 맵상 실제 위치에 있는지 눈으로 확인. 어긋나면 **Relocate(드래그 재위치)**
- [ ] 관제 지도에서 J1~J3 마커가 실제 현장 위치와 맞는지

---

## 11. 렉 없이 호출 테스트

1. 태블릿(J1)에서 **"로봇 호출 — 렉 없이"**
2. 로봇 도착 → **[다음 위치]** 로 J2 → J3 → J1 순환
3. 마지막에 **[작업 종료]** → 충전소 복귀까지 확인

### ⚠️ 확인 사항

- [ ] **"렉 포함" 버튼을 누르지 말 것** — 렉을 등록 안 했으므로 `501 rack_detection_error`로 실패합니다
- [ ] 호출을 **한 번에 하나씩** 하세요. 여러 화면에서 동시에 누르면 이중 배정 레이스(미해결이슈 E2/E3)가 재현될 수 있습니다
- [ ] 최소 3~4 사이클 돌려서 반복 재현되는지 확인
- [ ] 종료 후 충전 도킹이 되는지 — **시연 전에 복귀를 한 번 미리 테스트**하세요. `C1-1`(6-1)을 등록했으면 로봇이 경유지를 거쳐 정면으로 진입합니다. 그래도 `103 invalid_charge_dock`이 나면 `C1-1` 위치를 독 정면 축에 더 정확히 맞추거나 거리를 1.5m로 늘려보세요

---

## 12. 안 될 때 진단 (순서대로)

**1순위 — 로봇이 왜 안 움직이는지는 여기 다 나옵니다:**
```
http://192.168.30.100:8090/chassis/moves?page_size=5
```
`state`와 `fail_reason`을 봅니다.

| fail_reason | 의미 | 조치 |
|---|---|---|
| `506 jack_in_up_state` | 잭이 올라간 채 부팅 | rb-admin에서 `jack_down` |
| `501 rack_detection_error` | 그 자리에 렉이 실제로 없음 | 렉 없이 호출로 진행 |
| `9 calculation_failed` | 경로 계산 실패(공간 부족/출발점 무효) | 로봇을 여유 있는 곳으로 옮기고 재시도 |
| `103 invalid_charge_dock` | 충전독 인식 실패 | 로봇을 충전독 앞으로 옮겨 재시도 |
| `3 starting_point_out_of_map` | 위치추정 소실 | rb-admin Relocate |
| `18 rotate_timeout` | 회전 중 타임아웃 | 주변 장애물 확인 |

**2순위 — 로봇 상태:**
- 위치추정: rb-admin에서 로봇이 제 위치에 있는지
- 잭: `progress 0.0`이어야 정상 (0.0=내려감, 1.0=올라감, `hold`는 위아래와 무관)
- 경보: rb-admin 알람 확인 (관제 알림 내역에는 로봇 경보가 안 뜹니다 — 미해결이슈 10번)

---

## 13. 자주 겪는 함정 (8/5~8/7 경험)

- **충전 중 맵핑 시작 → 무한 "수신 대기 중"**: 충전기에서 분리하고 다시 시작 (8/7)
- **렉 들고 멈춤, 경로 안 생김**: 픽업 지점 여유 부족(0.46m < 필요 0.475m) → 잭 다운하면 즉시 이동. 렉 위치를 여유 있는 곳으로 (8/7)
- **로봇이 명령 거부(잭506)**: 잭 올라간 채 부팅됨 → rb-admin에서 `jack_down`
- **맵 떴는데 제자리(위치추정 소실)**: rb-admin에서 **Relocate(드래그 재위치)**.
  (충전소에 없을 땐 관제 "위치재조정" 자동버튼 쓰지 말 것 — 로봇이 C1에 도킹돼 있다고 가정하는 기능)
- **관제 마커 안 움직임**: 관제 맵에서 **"로봇 연결"** 눌러야 실시간 위치 WS 열림
- **rb-admin 검은 맵**: 데이터는 정상, 강력 새로고침 / current-map 재선택
- **POI만 바꿀 때**: "새맵생성" 말고 **오버레이만 갱신** 동기화
- **저장 후 "위치 정보를 찾을 수 없어요"**: POI id가 바뀐 것 → 슬롯·charging_id 재세팅 (7-1)
- **태블릿에서 접속 안 됨**: 방화벽 8002 인바운드 + Wi-Fi 프로필 Private (2-1)
- **DB timeout**: run_local.ps1(공장 .0.21) 실수 실행 → run_localdb.ps1로 재실행
- **노트북 IP가 바뀜**: DHCP라 재부팅 시 변경 가능 → 태블릿 앱 서버주소도 같이 바꿔야 함
- **[종료] 눌러도 로봇이 안 움직임**: `charging_id` 가 NULL 일 가능성 (8번 항목). `moves` 에 `creator: rcs` + `charge` 가 있는지로 판정 (8/10)
- **렉을 R1 앞에 내려놓음**: 8/10 수정 완료(`to_unload_point` 로 변경). 그래도 어긋나면 R1 주변 여유를 확인
- **저장했더니 태블릿이 "위치 정보를 찾을 수 없어요"**: POI id가 바뀐 것 → `active-pois` 로 새 id 확인 후 주소/슬롯/충전소 재설정 (7-1)
- **동기화했는데 `create_time` 이 옛날 날짜**: 정상입니다. 덮어쓰기라 안 바뀝니다 → `last_modified_time` 을 보세요 (7-2)
