# RCS Basic VESA 작업일지

> 매일 작업한 내용을 최신순(위쪽이 최신)으로 추가.

---

Noah 일일 업무 보고 <2026.08.10>

1. RCS Basic VESA 모델 개발

미해결이슈 3건 수정 (7·9번 실기 확인 완료, 8번 검증 대기)
- 7번 충전 중 맵핑: 충전독에 도킹된 채 맵핑을 시작하면 로봇이 요청은 받아주고 맵은 안 만드는 문제. `is_charging()` 신설(배터리 토픽만 3초 조회)해서 로봇에 요청 보내기 전 409로 차단 + 안내. 판정 불가면 통과시키는 fail-open 설계 — 오탐으로 정상 맵핑을 막지 않기 위함. **실기 확인 완료**
- 9번 맵핑 모달 무한 대기: `/map` 토픽이 비어 있어도 도착만 하면 폴백을 영구 차단하던 버그. 실데이터일 때만 잠그도록 수정 + WS 오류를 화면에 표시 + 15초 무수신 시 안내와 [다시 시작] 버튼 추가. **실기 확인 완료**
- 8번 `calculation_failed` 무한 재시도: 렉 적재 시 경로 계산에 실패하면 200회(약 17분) 재시도해서 태블릿에 안내가 한참 안 뜨던 문제. `safe_move`에 "같은 실패코드 3회 연속이면 중단" 옵션을 추가하고 배차 이동에만 적용(단발 실패는 기존대로 회복). 렉 적재 시에는 코드 9 문구를 "공간이 부족" 으로 분기. **모의 테스트 14/14 통과, 실기 재현 못 해 검증 대기**

렉 반납 정밀화 (실기 확인 완료)
- 증상: [작업 종료] 시 렉을 R1이 아니라 그 앞에 내려놓음
- 원인 규명: 이동 명령은 R1 좌표로 정확히 나갔고 로봇도 succeeded로 응답했는데 실제 위치는 0.717m 앞이었음. `standard` 이동은 목표 근처 도달로 성공 처리하며 정밀 정렬을 하지 않음. 레거시 코드는 같은 동작에 하역 전용 타입(`to_unload_point`)을 쓰고 있었고 VESA만 `standard` 였음
- 조치: 렉 반납 이동을 `to_unload_point` 로 변경 + 실패 시 기존 방식 폴백. 실기에서 정위치 반납 확인

충전소 자동 복귀 복원 (실기 확인 완료)
- 증상: [작업 종료] 후 로봇이 그 자리에 대기. 에러 없이 세션은 completed
- 원인 규명: `charging_id` 가 NULL이면 복귀 블록 전체를 건너뜀. 이동 기록의 `creator` 를 대조해 8/7에는 백엔드(`rcs`)가 도킹 명령을 냈고 8/10에는 전부 수동(`robot-admin-web`)이었음을 확인
- 조치: `charging_id` 재설정 + 경유점 이름을 `C1-1` 로 맞춤(코드가 이름으로 찾음). 종료 → 경유 → 도킹까지 전 구간 `creator: rcs`, `fail_reason: 0`, 충전 중 상태 확인

POI id 파괴 메커니즘 규명 (미해결이슈 5번)
- 맵을 저장하면 POI가 delete+재생성되며 id가 바뀌는 것에 더해, **외래키가 `ON DELETE SET NULL` 이라 `robots.charging_id` 가 DB에 의해 자동으로 지워진다**는 것을 확인
- 이름 한 글자만 바꿔 저장해도 동일하게 발생. 당일 2회 실제 피해(충전소 설정 소실, 태블릿 주소 무효화)

배차 동시성 수정 (E1·E2·E3, 검증 대기)
- 배정 경로(점유 검증 → 로봇 선정 → 세션 생성 → 워커 등록)를 단일 락으로 원자화. 여러 태블릿이 동시에 누를 때 같은 로봇/같은 POI가 이중으로 잡히던 문제
- 로봇이 모두 바쁘면 거부하지 않고 대기열에 등록 → 먼저 끝난 로봇이 FIFO로 자동 배정. TTL 30분, 중복 등록 거부, 취소 API 추가
- 태블릿: 가용 0대여도 호출 버튼을 막지 않고 "대기열 N번째" 표시 + 대기 취소 버튼
- 실제 서비스 코드를 그대로 태우는 테스트 23/23 통과. 락을 무력화한 대조군에서는 25/25 라운드 이중배정 발생 — 테스트가 실제로 레이스를 검출함을 확인

맵 동기화 판정 기준 정정
- 로봇의 `create_time` 이 옛 날짜로 남아 "옛날 맵 사용 중"으로 오판했으나, 덮어쓰기 동기화에서는 `create_time` 이 갱신되지 않음을 확인. 맵 이미지 md5·grid_origin·POI 좌표 대조로 최신 맵이 정상 적용돼 있음을 확정하고, 판정 기준을 `last_modified_time` 으로 문서 수정

문서 갱신
- `미해결이슈.md`: 상태 범례에 "코드 완료·검증 대기" 추가, 해결완료 항목 신설, 검증 대기 목록 정리
- `현장세팅_체크리스트.md`: 동기화 판정 기준·POI id 경고·충전소 지정 판정법 갱신

---

Noah 일일 업무 보고 <2026.06.19>

1. RCS Vesa 개발
    태블릿 — 도착 시 화면 전환 즉시화
     - 도착 후 잭다운 sleep(8초) 동안 state=empty로 잡혀서 다음 명령 화면이 한참 뒤에 뜨던 문제
     - `_set_db_status("awaiting_next")`를 잭다운 전에 호출 → 폴링이 즉시 arrived 인식

    태블릿 — 다음 위치 보낸 후 풀스크린 출발 알림 (2단계)
     - 빈 화면으로 바로 전환되어 사용자가 "보냈다"는 인지 안 됨 → 풀스크린 오버레이
     - 1단계 `⟳ 출발 준비 중` (8초, pulse ripple + border spinner)
     - 2단계 `✓ {POI} 으로 출발` (2초)
     - 백엔드 잭업 끝나는 시점과 동일하게 8초 후 ✓ 전환

    워커 흐름 — 잭업 후 status="moving" 설정
     - 기존: `_set_target_poi` → `status=moving` → `_jack_up_step` (잭업 전에 moving 진입)
     - 변경: `_set_target_poi` → `_jack_up_step` → `status=moving` → `_move_to_poi`
     - status="moving" 시점이 **실제 이동 시작 시점**과 일치 (UI 상태와 정합)

    잭 안정화 시간 조정
     - JACK_SETTLE_SEC: 10초 → 5초 → 잭 미완료 출발 발생 → **8초로 고정**
     - 잭 모터 동작 4-6초 + 안전 buffer 2-4초

    태블릿 spinner 정중앙 정렬 수정
     - ⟳ 글자 회전이 폰트 baseline 차이로 비대칭 → 글자 빼고 **CSS-only border spinner**
     - `top:50%; left:50%; margin:-32px 0 0 -32px`로 박스 정중앙 위치
     - 박스 자체는 ripple pulse, 안의 spinner는 회전 (두 애니메이션 독립)

    자체 confirm 모달 + 종료 버튼 모드별 텍스트
     - 브라우저 alert/confirm → 다크 톤 자체 모달 (`confirmModal` Promise)
     - with_rack 모드별 텍스트 분기: "(렉 반납 + 충전소 복귀)" vs "(충전소 복귀)"

2. Zishen 자율주행 개발
    장비 확인
     - 라이다(Livox Mid-360S)랑 로봇 IP 찾음
    ROS2 설치
     - Humble + Zenoh 깔고 네트워크 고정
    라이다 연결
     - ROS2로 점군 받기 성공
    SLAM 빌드 
     - 네비 스택 20개 패키지 빌드
    매핑 성공
     - 라이다로 지도 만드는 거(FAST-LIO) 작동 확인
---

Noah 일일 업무 보고 <2026.06.18>

1. RCS Vesa 개발

TabletApp Debug APK 빌드 + VESA 아이콘
- 다크 네이비 그라데이션 + 흰색 V + 초록/파란 점 디자인
- Adaptive Icon (Android 8+) + PNG 5종 (Python PIL 생성)

호출 시 렉 유무 선택 + 태블릿 호출 버튼 2개 분리
- `dispatch_sessions.with_rack` 컬럼 추가, body로 받음
- 빈 상태에서 `🟦 렉 포함` / `⬜ 렉 없이` 2개 버튼

잭 동작 with_rack 무관 항상 동작
- 매 도착 시 jack_down(작업 대기), 매 출발 시 jack_up(운반)
- 렉 없이 모드도 잭 사이클 유지 (안전 이동)

가용 로봇 수/선정 ONLINE 체크
- 오프라인 로봇 자동 제외 → 표시 N대 = 실제 호출 가능 N대
- `fetch_all_robots_live` 16-worker 병렬 사용

맵 동기화 시 robot.area_id 자동 갱신 + "층 전환" 버튼 제거
- `api_sync_map_to_robot`에서 타겟 로봇 area_id 자동 업데이트
- 관제의 수동 층 전환 버튼 제거 (불필요)

OperationsDashboard 신규 (관제 우측 패널)
- 4섹션: 작업 위치 / 로봇 상태(한줄 카드) / 오늘 KPI / 최근 이벤트
- 데이터 소스 4개 병렬 폴링 (dispatch/status, robots/live, active-pois, sessions/today)

로봇별 고유 색상 8종 팔레트
- robot_id로 자동 매핑 — 작업 위치/충전소 어디서든 동일 색
- 점유 시 진한 색 + 두꺼운 테두리 + 그림자, 비어있음 회색 점선

KPI 데이터 소스 변경 + 패널 이름 변경
- `task_history` → `dispatch_sessions/today` (VESA 흐름은 task_history 미사용)
- 신규 API `GET /api/dispatch/sessions/today`, 우측 패널명 "운영 현황"

자체 confirm 모달 + 종료 버튼 모드별 텍스트
- 브라우저 alert/confirm → 다크 톤 자체 모달 (`confirmModal` Promise)
- with_rack 모드별: "(랙 반납 + 충전소 복귀)" vs "(충전소 복귀)"

태블릿 깜빡임 fix
- 도착 직후 잭다운 10초 동안 state=empty로 잡히던 문제
- `_poi_status` arrived 조건 확장 (moving + target=NULL도 포함)

태블릿 레이아웃 fix
- POI 그리드 stretching → `grid-auto-rows: 110px` + `align-content: start`
- 강제 제어 버튼 좌측 → 우측 하단, 종료 버튼과 겹침 해결

버그: 렉 없이 호출 시 첫 POI 이동 누락
- with_rack 분기 들여쓰기 실수로 첫 POI 이동까지 픽업 안에 들어가 있던 것
- 수정: 첫 POI 이동 블록을 분기 밖으로

rcs_basic_db → rcs_vesa_db 재복제
- vesa에 어제 추가한 맵/POI 데이터 없어서 전체 재동기화
- 19 테이블 LIKE+INSERT + FK 23개 복원

---

Noah 일일 업무 보고 <2026.06.04>

1. RCS Vesa 개발

RCS-Basic 메인 코드 전체 동기화
- 메인(`RCS-Basic`)의 발전된 코드를 VESA로 흡수: `jack_service.py` +70줄, `scheduler.py` +32줄, `robot.py` +17줄, 신규 `routers/settings.py`, `autoxing_api/`, `robot_api/`, `scripts/`
- VESA 고유는 보존: `dispatch_*` 6개 파일 (모델/스키마/CRUD/서비스/라우터/HTML)
- VESA 패치 재적용: `main.py`(dispatch 라우터 + recover_on_startup 훅), `models/__init__.py`(DispatchSession/TabletSlot import), `constants/robot_types.py`(interactive_relay work_mode), `SideNav.tsx`(작업관리 메뉴 제거), `MonitoringClient.tsx`(DispatchTabs 제거), `MainActivity.kt`(슬롯 URL)
- 운영 데이터 보존: `default_area.json`, `static/maps`, `frontend/.env.local`
- 검증: 백엔드 import OK, jack_service 시그니처 호환(`_get_standby_poi`, `safe_move`, `jack_up`, `jack_down`, `update_job_status`, `_running_robot_id_by_ip` 등 8개), robot.py 강제 제어 API 4개(`jack`, `cancel-move`, `dock`, `return-to-standby`) 호환

DB 전환 (rcs_basic_db → rcs_vesa_db)
- `.env`의 `DB_NAME`을 `rcs_vesa_db`로 재설정 (전체 동기화로 RCS-Basic 기본값 `rcs_basic_db`로 덮였던 것 복구)
- `rcs_basic_db`의 `tablet_slots`(슬롯 1: poi_id=734 "작업1") + `dispatch_sessions` 데이터를 `rcs_vesa_db`로 이전
- Docker 백엔드 재시작 필요 (`docker-compose up -d --force-recreate backend`) — 컨테이너는 시작 시점 환경변수 캡처

VESA 충전소 도킹 — 2단계 흐름으로 변경 (RCS-Basic과 동일화)
- 기존: `_return_to_standby_and_park`가 jack_down 후 곧장 `charge` 호출 (1단계, 사전 접근 없음)
- 변경: standby → jack_down → **사전 접근(`<charger_name>-1` POI standard 이동, best-effort)** → **charge 도킹** (2단계)
- 신규 헬퍼 `_find_charge_approach_poi(area_id, charger_name)` — 같은 area 활성 맵에서 `{name}-1` POI 검색, 없으면 충전소 좌표 그대로 사전 접근에 사용
- 1단계는 실패해도 charge 단계로 진행 (RCS-Basic 정책과 동일)

---

Noah 일일 업무 보고 <2026.05.29>

1. RCS Vesa 개발

VESA 태블릿 — 슬롯 시스템 도입
- 태블릿이 외우는 ID를 POI ID(410, 411 …) → **슬롯 번호(1, 2, 3 …)** 로 단순화
- `tablet_slots` 테이블 신규 (slot_number PK, poi_id FK, alias optional)
- API: `GET/PUT/DELETE /api/dispatch/slots/{n}`, `GET /api/dispatch/slots`, `GET /api/dispatch/slots/available-pois`
- URL: `GET /api/dispatch/tablet/{slot}` — 매핑 없으면 페이지 내에서 **설정 모드** 자동 표시 (POI 드롭다운 + 별명 + 저장)
- 헤더 우상단 ⚙ 버튼으로 언제든 슬롯 매핑 재설정 가능
- 기존 `GET /api/dispatch/tablet/poi/{id}` 는 호환용으로 유지
- TabletApp: 설정 다이얼로그 "POI ID" → "슬롯 번호" 로 변경, prefs 키 `poi_id` → `slot_number`

라우팅/UX 후속 수정
- 레거시 `/tablet/{robot_id}` 라우터 제거 (슬롯 라우터와 경로 충돌로 "레거시 모드" 페이지가 떴던 문제 해결)
- `/slots/available-pois`를 `/slots/{slot_number}`보다 먼저 정의 (FastAPI 라우팅 우선순위 충돌 해결 — "available-pois"가 int로 파싱되던 422 에러 수정)
- **슬롯 설정 POI 드롭다운**: 맵관리 "메인 적용"된 area의 활성 맵 POI만 표시 (`_current_area_id` 활용). 맵 적용 변경 시 자동 반영

---

Noah 일일 업무 보고 <2026.05.28>

1. RCS Vesa 개발

GitHub 저장소 분리
- 기존 `UND_RCS_Basic` origin을 `RCS_Basic_VESA_ver`로 교체하고 `feature/backend_noah` 브랜치 push (커밋 `33bc638`, 658 파일 변경)
- `.gitignore`에 `업무보고_*.md` 패턴 추가

VESA 운영 방식 설계 (인터랙티브 배차)
- 로봇 3대 동시 운영, 작업 포지션 3~10개
- 잭은 시작 시 1회 업, 종료 시 1회 다운 (포지션마다 잭 사이클 없음)
- 첫 작업 포지션만 사전 지정, 다음 위치는 매번 태블릿에서 지시
- 종료 명령 시 standby로 복귀 → 잭다운 → 충전소 도킹

VESA 모드 백엔드 구현
- `models/dispatch.py` — `dispatch_sessions` 테이블 (상태 머신 영속)
- `schemas/dispatch.py`, `crud/dispatch.py` — Pydantic + CRUD
- `services/dispatch_service.py` — 로봇당 워커 스레드 1개, `threading.Event` 기반 next/end
- `routers/dispatch.py` — API 5개 (`start`, `next`, `end`, `status`, `tablet`)
- `templates/dispatch_tablet.html` — POI 그리드 (점유 비활성), 2초 폴링
- `main.py` — 라우터 등록 + 재시작 복구 훅 (`recover_on_startup`)
- `constants/robot_types.py` — `interactive_relay` work_mode 추가
- `TabletApp/MainActivity.kt` — URL을 `/api/dispatch/tablet/{id}`로 변경

운영 방식 변경 — 위치별 태블릿 호출 방식으로 재설계
- 관제 일괄 시작 폐기 → 각 작업 위치 태블릿에서 [호출] → 가용 로봇 자동 배정
- 가용 로봇 선정: `robot_status.battery_level` 내림차순 (충전 많은 순), 없으면 호출 거부
- 도착 후 같은 위치 태블릿에서 비어있는 다음 위치만 선택 가능
- 종료 버튼: 그 위치에 있는 로봇만 standby + 잭다운 + 충전소 복귀

신규 코드
- `dispatch_service.py` — `find_available_robot`, `get_session_at_poi`, `get_session_heading_to_poi`, `call_to_poi` 추가
- `schemas/dispatch.py` — `DispatchPOIStatusOut`, `DispatchCallResult` 추가
- `routers/dispatch.py` — POI 기준 API 5개 (`/poi/{id}/call`, `/poi/{id}/next`, `/poi/{id}/end`, `/poi/{id}/status`, `/tablet/poi/{id}`)
- `templates/dispatch_tablet.html` — POI 기준 UI로 전면 재작성 (empty: 호출 / calling: 대기 안내 / arrived: 다음 위치 그리드 + 종료)
- `TabletApp/MainActivity.kt` — 설정 항목 "로봇 ID" → "작업 위치 POI ID"

DB 마이그레이션 (rcs_basic_db → rcs_vesa_db)
- 18개 테이블 복제 (행 수 100% 일치)
- FK 22개 복원 (`CREATE TABLE LIKE`는 FK 미복사 — ALTER로 별도 추가)
- 백엔드 `DB_NAME=rcs_vesa_db`로 재시작 안내

VESA 태블릿 — 강제 제어 모달 추가 (5/29 추가됐던 항목 — 이후 위치 변경됨)
- `/api/dispatch/poi/{id}/status`에 `robot_ip` 필드 노출 → 프론트가 직접 기존 `/api/robots/remote/*` 호출
- 헤더 우상단 [강제 제어] 버튼 → 모달 (잭 업/다운, 이동 취소, 충전소 복귀, 대기장소 복귀)
- 자동 동작 중 멈춤/오작동 시 사용. 로봇이 이 위치에 있을 때만(`robot_ip` 있을 때) 버튼 활성
- 별도 백엔드 wrapper 라우터 만들 필요 없이 기존 `robots/remote` 라우터 재사용

프론트엔드 메인화면 정리
- `MonitoringClient.tsx`의 단일/배치 수동배차 탭(`DispatchTabs`) 제거
- 시작/배차는 위치별 태블릿이 담당 → 좌측 하단은 `ActiveJobsPanel`(진행 중 로봇 카드)만 유지
- `JackTestPanel`, `BatchDispatchPanel` 컴포넌트 파일은 보존 (재사용 가능성)
- 사이드바에서 "작업관리" 메뉴 제거 (자동 경로/스케줄은 VESA 미사용) — `/tasks` 페이지 파일은 보존
