# RCS LG 작업일지

> 매일 작업한 내용을 최신순(위쪽이 최신)으로 추가.

---

Noah 일일 업무 보고 <2026.08.07>

1. RCS LG 개발 (동기화 위치 보정 · 부팅 자동 복구 · 배차 안정화 · 강제 정리 기능)

    동기화 후 위치 보정 완성 및 맵 설정 안정화
     - current-map 재선택이 LTE·펌웨어 지연으로 한 번에 안 먹어 맵이 설정 안 되던 문제 → 설정 여부를 확인하며 최대 3회 재시도하도록 보강
     - 동기화 시 맵 재선택 전 로봇 포즈를 저장했다가 재선택 후 복원해 위치가 리셋되지 않도록 처리하고, 유휴(충전소 도킹) 로봇은 동기화 후 충전소 기준으로 정확히 위치 재조정

    로봇 부팅 후 자동 복구 추가
     - 로봇 부팅 시간이 30초~1분으로 길어지면서 부팅 직후 맵 미설정·위치 리셋되던 문제 대응
     - 로봇이 오프라인→온라인(부팅 완료)으로 전환되면 자동으로 current-map 설정 + 충전소 기준 위치 재조정을 수행하는 백그라운드 복구 기능 구현(작업 중인 로봇은 도킹 위치가 아닐 수 있어 제외)

    위치재조정 방향(헤딩) 오류 수정
     - 충전소 기준 위치재조정 시 로봇 방향이 180° 뒤집히던 문제 수정 — 충전소 POI 방향을 그대로 로봇 헤딩으로 적용(수동 재조정·부팅 자동 복구 양쪽 반영)

    인터랙티브 배차 안정화
     - 방금 떠난 위치(예: J1)를 곧바로 호출/예약할 때 "점유중"으로 거부되고 화면이 롤백되며 깜빡이던 문제 수정 — 다른 곳으로 이동 중인 로봇의 출발지를 점유 판정에서 제외
     - 실행 중인 배차 작업을 로봇 이동 없이 즉시 정리하는 "작업 강제 종료" 기능을 관제 메인화면·콘솔의 로봇별 원격제어에 추가(로봇은 물리적으로 그대로 두고 세션만 종료 → 로봇 즉시 재사용 가능, 충전소 복귀는 별도 버튼)

2. 달서구청 그림그리기 로봇 개발 (별도 협동로봇 프로젝트)

    앱 종료 시 로봇 전원 처리 점검 및 안전 높이 상향
     - 종료 로직 확인: 로봇 전원 OFF(cobot_power.py servo_off)에는 안전 위치로 이동 후 전원 차단하는 로직이 있으나, 앱의 종료 버튼(SystemOff)은 로봇 전원 처리가 없어 서보 ON 상태로 잔류하던 문제 확인
     - 조치: 전원 차단 전 이동 높이가 낮아 안전 여유가 부족했던 것을 보완 — cobot_power.py(약 97행) 이동 Z값을 400 → 500으로 상향(XY·자세는 유지), dist_draiwin/python 배포 복사본에도 동일 반영

---

Noah 일일 업무 보고 <2026.08.06>

1. RCS LG 개발 (LG 2호 랙 스펙 추가 · 랙 인식 튜닝 · 배차 콘솔 확장 · 배차/동기화 버그 수정)

    LG 대차 2번째 버전(LG2) 랙 스펙·POI 추가
     - 도면(다리중심 665×600, 캐스터 GDS-100) 기준 LG2 랙 스펙을 신규 추가하고, 프론트 POI 편집 화면에 LG2 사이즈 선택지 추가
     - 동기화 시 맵에 등록된 POI 사이즈 종류대로 rack.specs 를 로봇에 전송하도록 구성(LG·LG2 혼재 시 두 스펙 모두 반영)

    랙 인식 실패 디버깅 및 rack.specs 실측 튜닝 — "Wrong rack size" 반복 문제 해결
     - 원리 규명: rack.specs 의 width/depth 는 도면 치수가 아니라 로봇 라이다가 실제로 재는 "다리 중심 간격(detected)" 에 맞춰야 매칭이 통과함을 확인하고 실측값 기준으로 재설정
     - 다리 오인식 수정: 실측 다리 지름 25mm 에 맞춰 leg_size 50mm→25mm, foot_radius 0.0125 로 조정 → 다리 옆 캐스터(약 50mm)를 다리로 잡던 문제 해소
     - 체인 간섭 규명: 랙 중앙에 늘어져 바닥에 끌리는 체인이 라이다 평면을 가로질러 depth 가짜값(0.37·0.47)을 만들던 것을 확인 → 물리적으로 체인 정리 필요(소프트웨어 필터 불가)
     - 뒷다리 검출 편차 대응: 체인 제거 후에도 캐스터 간섭으로 depth 가 0.52~0.57 로 흔들려, 관측 범위 중앙값(0.545)으로 설정해 펌웨어 허용오차 안에 수렴시킴
     - 캐스터 장애물 오인 대응: 튀어나온 캐스터가 진입 시 장애물로 잡히던 문제를 margin 0.08(랙 풋프린트 확장)로 완화

    배차 콘솔 기능 확장
     - 콘솔 우측에 등록 로봇 사이드바 추가 — 로봇별 온라인 여부·배터리·작업 상태 표시, 카드의 [직접 제어]로 관제와 동일한 원격제어(방향 조종·잭 업/다운·충전소/대기장소 복귀·시스템 재시작) 모달 제공
     - 콘솔에서도 로봇 부착 태블릿의 [확인]을 대신 눌러 다음 경유지로 진행/종료할 수 있게 추가

    인터랙티브 배차 버그 2건 수정
     - 출발지가 "경유지 예약"으로 잘못 표시되던 문제 — 이동 중인 로봇의 현재 위치를 예약 표시에서 제외하고, 미래 목적지(다음 경유지)만 예약으로 표시하도록 분리
     - 출발 직후 화면이 롤백(호출 버튼이 깜빡)되던 문제 — 경로 등록이 워커 스레드로 비동기 처리되며 폴링이 이전 상태를 보여주던 것을, 출발 상태를 동기적으로 먼저 반영하도록 수정

    맵 동기화 후 로봇 위치 리셋 문제 해결
     - 원인: 동기화 과정의 current-map 재선택(오버레이 리로드)이 로봇을 리로컬라이즈시켜 현재 위치가 초기화되던 것으로 규명
     - 해결: 재선택 직전 로봇 포즈를 WebSocket(/tracked_pose)으로 저장한 뒤, 재선택 후 POST /chassis/pose 로 복원해 위치가 유지되도록 처리(오버레이·랙 동기화에 적용, 실기 왕복 검증 완료)

---

Noah 일일 업무 보고 <2026.08.04>

1. RCS LG 개발 (LTE 환경 매핑·로봇 통신 디버깅)

    매핑 미동작 원인 추적 — LTE(M2M)로 연결 시 매핑 화면에 맵·로봇 위치가 안 그려지던 문제
     - 증상 정리: 같은 라우터(로컬망)에서는 매핑이 되고, 서로 다른 라우터(LTE M2M)로 연결할 때만 매핑이 안 되던 상태에서 원인 추적
     - 통신 경로 정밀 측정: 로봇 제어·상태·ping 같은 소량 데이터는 되는데 매핑 실시간 데이터(스캔·맵·위치)만 안 오는 것을 확인하고, 처음엔 대량 전송 대역폭 문제 또는 MTU 문제로 의심
     - MTU 배제: M2M 경로에서 최대 크기(1500) 패킷도 손실 없이 통과함을 ping(DF)으로 확인해 MTU 문제가 아님을 확정
     - 로봇 SLAM 상태 점검: 매핑 세션은 생성되나 로봇이 위치를 못 잡는(lidar_matched=false) 상태를 발견, 위치 초기화(global positioning)로 위치 추정이 살아나는 것을 확인

    진짜 원인 규명 — LTE 유동 IP로 인한 로봇 접속 단절
     - 로봇 라우터 WAN IP가 재부팅·네트워크 설정 변경마다 계속 바뀌는 것(하루에 10.230 → 10.49 → 10.25 등 여러 차례) 확인
     - 등록된 옛 IP로는 라우터까지 ping은 되지만(그 IP가 다른 M2M 장비에 재할당됨) 로봇 8090 API에는 연결되지 않아, 매핑·상태·정보 조회가 모두 실패하던 것으로 판명
     - 로봇 내부 IP(192.168.39.100:8090)로 직접 접속해 로봇 API 자체는 정상(200 응답, nginx, 맵 2FF 유지)임을 확인 → 로봇·서비스 문제가 아니라 접속 경로(IP) 문제로 확정
     - 현재 WAN IP로 재설정 후 재검증: device 정보 조회(모델 longjack, 펌웨어 2.12.28), 매핑 스캔(/scan_matched_points2)·로봇 위치(/tracked_pose)·맵 토픽이 모두 정상 수신, 맵 이미지(대용량) 다운로드도 정상 → 그동안의 매핑 미동작·대량 전송 실패·정보 조회 실패가 전부 IP 불일치가 원인이었음을 규명

    부수 확인 및 정리
     - 앞서 세운 가설(device/info 경로가 LTE에서 막힘, 매핑 대역폭 한계, 펌웨어/토픽 불일치)은 모두 IP 불일치가 만든 착시였음을 정정
     - 포트포워딩 규칙(192.168.39.100 / 8090 / 8090 / TCP)은 정상임을 재확인
     - 남은 핵심 과제: LTE 회선이 유동 IP라 접속이 계속 끊기는 문제 → 고정 IP 확보 또는 라우터 DDNS(고정 이름) 설정이 실운영 전 필수임을 정리

---

Noah 일일 업무 보고 <2026.08.03>

1. RCS LG 개발 (LTE 네트워크 전환 세팅 및 로봇 통신 디버깅)

    LTE 전환 네트워크 구성 및 통신 검증 — WiFi 로컬망에서 LTE(M2M) 망으로 로봇 통신 전환
     - 보유 라우터 확인: ESTLA TC700(관리 화면 브랜드 RED Dolphin), LTE CAT.4 / LAN 2포트 / WiFi 2.4GHz. VPN 메뉴는 없고 포트포워딩·DDNS는 지원
     - 회선 성격 파악: 유심 APN이 m2m-router.lguplus.co.kr(LG U+ M2M 전용 APN)임을 확인. WAN IP가 10.x 사설 대역(CGNAT)이라 공인 IP 직접 접속은 불가하지만, 같은 전용 APN 장비끼리는 통신 가능한 구조로 정리
     - 장비 간 통신 실증: 서버 PC 라우터(WAN 10.240.x)에서 로봇 라우터(WAN 10.230.x)로 ping 성공 확인 → 전용 APN 사설망에서 장비 간 통신이 허용됨을 검증(장비 격리 아님)
     - 포트포워딩 설정: 로봇 라우터에 8090 포트를 로봇 내부 IP(192.168.39.100)로 포워딩 → 서버에서 로봇 제어 API(/chassis/*, /services/*)와 WebSocket이 LTE로 정상 접속됨을 확인
     - CGNAT / 전용 APN / 포트포워딩 개념 정리 및 로봇마다 라우터를 두는 구성안, 서버 PC를 같은 APN 망에 연결하는 방법 정리

    백엔드 LTE 대응 수정 — LTE 지연·특정 경로 응답 불가에 맞춘 통신 로직 보정
     - /device/info 응답 불가 발견: LTE 경로에서 /device/info 만 응답이 오지 않음(25초 0바이트, 응답 크기 무관). 이 경로에 의존하던 온라인 판정·맵 연결이 모두 실패하던 문제 해결
     - 온라인 판정 경로 교체: 무거운 /device/info 대신 가벼운 /chassis/current-map 으로 판정하고, 맵이 없어 404 가 와도 '응답이 오면 온라인'으로 처리하도록 수정(로봇관리·맵관리 양쪽)
     - 원격 조종 속도 개선: 방향키(twist) 명령마다 WebSocket 을 새로 열어 LTE 에서 회당 0.5~1.7초 걸리던 것을, 연결을 재사용하도록 변경해 최초 연결 후 전송이 거의 0초로 단축
     - 타임아웃 전반 상향: 로봇 통신 타임아웃을 LTE 지연에 맞춰 상향(2·3초→8초, 5초→10초 등), 이동 폴링 주기·상태 캐시도 조정해 오프라인 오판·명령 유실 방지
     - 라이브 온라인 체크에 IP별 TTL 캐시를 두어 매 요청마다 수 초 걸리던 상태 조회를 완화

    로컬 개발 DB 구성 — 원격 rcs_vesa_db 를 로컬 rcs_lg_db 로 복제
     - 192.168.0.21(MariaDB)의 rcs_vesa_db 를 로컬(MySQL 8) rcs_lg_db 로 복제: 21개 테이블 전체 + FK 26개 + 데이터 이관, MariaDB→MySQL 방언 차이 보정
     - 백엔드 실행용 환경변수 스크립트(run_local.ps1) 정리 — DB 이름 미지정 시 구 DB에 붙어 500 오류가 나던 문제 방지

    로봇 원격제어·맵·매핑 실동작 디버깅
     - 로봇 실제 상태를 WebSocket/REST 로 직접 측정해 어떤 경로가 LTE 로 되고 안 되는지 전수 확인(제어·상태·배터리는 정상, /device/info 만 불가)
     - 매핑 진단 중 로봇 current-map(현재 맵)이 풀린 것을 발견해 2FF 맵으로 복구
     - 매핑 이슈 확인: 매핑 세션은 생성되나(POST 201) 로봇이 실제 매핑 모드로 전환되지 않고, 실시간 스캔·맵·위치 데이터가 오지 않음. lidar_matched=false(로봇 위치 미확보) 및 코드가 기대하는 토픽(/tracked_pose·/trajectory 등)과 로봇 실제 토픽 불일치 확인
     - 원인 분리: 통신(LTE/WebSocket)은 정상이며, 매핑 미동작은 네트워크 속도가 아니라 로봇 SLAM 상태·펌웨어/모델 차이 문제로 판단(로봇 SN 체계가 기존과 다름 — B 계열). 로봇 소프트웨어 업데이트 후 재검증 예정
