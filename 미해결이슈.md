# RCS Basic VESA — 미해결 이슈

> CLAUDE.md에서 분리(2026-08-06). 이슈가 해결되면 상태를 🟢 해결완료로 바꾸고 해결 내용/날짜를 적는다.
> 상태 범례: 🔴 미해결 · 🟡 완화(우회 동작 중, 근본 미해결) · 🔵 코드 수정 완료·실기 검증 대기 · 🟢 해결완료(실기 확인)
>
> **표기 규칙(2026-08-07 도입)**: 근거를 **[측정]**(로봇/코드에서 직접 확인한 값) 과 **[추정]**(그 값으로부터의 해석·가설) 으로 구분해 적는다.
> 코드 참조는 `파일:줄` 로 적되, 코드가 바뀌면 줄이 밀리므로 인용문도 함께 남긴다.
> 2026-08-07 전수 재검증 완료 — 아래 항목의 코드 참조·수치는 그날 기준으로 실제 확인된 것.
> **2026-08-10 갱신** — 실기(로봇 Qs/192.168.30.101)로 확인한 것과 모의 테스트만 한 것을 🟢/🔵 로 구분했다.

## 2026-08-10 현재 상태 요약

| 상태 | 항목 |
|---|---|
| 🟢 **실기 확인 완료** | 7번(충전 중 맵핑 차단) · 9번(맵핑 모달 무한대기) · 3번(`to_unload_point` 동작 확인) · **신규 A**(렉 반납 정밀화) · **신규 B**(충전소 자동 복귀 + `C1-1` 경유) |
| 🔵 **코드 완료 · 검증 대기** | 8번 조치안 3(`calculation_failed` 조기 중단 + 렉 문구) · E1/E2/E3(대기열 + 배정 락) |
| 🔴 **미해결** | 1 · 2(AutoXing 문의 필요) · 4(레거시) · **5(POI upsert — 오늘 2회 실제 피해)** · 10(로봇 경보, 보류 결정) |
| 🟡 완화 | 6 · E4~E9 |

---

## 🔵 코드 수정 완료 · 실기 검증 대기

### [8번 조치안 3] `calculation_failed` 무한 재시도 제한 + 렉 문맥 문구
- **수정(2026-08-10)**
  - `jack_service.safe_move()` 에 `stop_on_repeated_fail: int | None = None` 추가 — 같은 `fail_reason` 이 N회 **연속**이면 지속성 실패로 보고 즉시 반환. 기본 None 이라 기존 호출부 동작 불변.
  - `dispatch_service._move_to_poi` 가 `stop_on_repeated_fail=3` 으로 호출 → 같은 코드 3연속(≈15~20초)이면 중단되어 태블릿 안내 카드가 뜬다. 이전엔 상한 200회(≈17분)를 다 돌았다.
  - `failure_guide` 에 `FAILURE_GUIDE_WITH_RACK` 오버라이드 신설 — 렉 적재 시 코드 9는 "렉을 실은 상태로는 지나갈 공간이 부족해요 / 렉 자리 주변 0.6m 이상 확보" 로 표시. `guide_for(key, *, with_rack=False)`.
  - 라우터가 세션의 `with_rack` 을 문구 조회에 전달(`routers/dispatch.py` 2곳).
- **[측정]** 모의 테스트 `tests/test_safe_move_stop.py` **14/14 통과** — 실패코드 9를 100회 주는 로봇을 모의해 3회에서 중단(대기 10초)되는 것, 단발 실패·코드가 바뀌는 실패·timeout 혼입 시에는 중단되지 않는 것 확인.
- **미검증**: 실기에서 `fail_reason 9` 를 재현하지 못했다(2026-08-10 주행 중 전 구간 `fail_reason 0`).
  → **검증 방법**: 렉을 주변 여유 0.5m 미만인 좁은 자리에 두고 "렉 포함" 호출 → ~20초 내 공간 부족 카드가 뜨는지.

### [E1 + E2/E3] 호출 대기열 + 배정 락
- **수정(2026-08-10)** — `dispatch_service.py`
  - `_assign_lock = threading.RLock()` 도입. `start_session` 전체와 `call_to_poi` 전체(점유검증 → 로봇선정 → 세션생성 → 워커등록)를 감싸 원자화. 락 순서는 `_assign_lock → _workers_lock` 한 방향만 존재(데드락 없음).
  - 대기열 `_pending`(FIFO) + `PENDING_TTL_SEC = 30분`. 로봇이 없으면 거부 대신 등록 → 실패키 `QUEUED`(중복은 `ALREADY_QUEUED`).
  - `_worker_loop` 의 `finally` 에서 `_remove_worker` 직후 `_drain_pending_queue()` → 먼저 끝난 로봇에 FIFO 자동 배정. 폐기 조건: TTL 초과 / 그 POI에 이미 세션이 생김.
  - `pending_position()` · `cancel_pending()` + `POST /poi/{id}/cancel-queue`.
  - 태블릿: 가용 0대여도 호출 버튼을 막지 않고(누르면 대기열 등록), 대기 중이면 "대기열 N번째" + [대기 취소] 표시. `poi/{id}/status` 에 `queued_position` 추가.
- **[측정]** `tests/test_dispatch_race.py` **23/23 통과**(실제 `dispatch_service` 를 import 하고 DB/로봇/스레드만 모의). 8스레드 동시 배정 25라운드에서 이중배정 0건, 같은 POI 동시 호출은 항상 1건만 성공, 서로 다른 POI 4건은 모두 성공(과잉 차단 없음), 대기열 FIFO·TTL·취소 동작 확인.
- **[측정] 대조군** — 같은 테스트에서 `_assign_lock` 만 무력화하면 **25/25 라운드에서 이중배정 발생**. 테스트가 레이스를 실제로 검출하며 락이 그것을 막고 있다는 증거.
- **미검증**: 로봇 2대 실기 동시 운영. → **검증 방법**: 로봇 1대만 켠 채 J1 호출(배정) → J2 호출("대기열 1번째") → J1에서 [작업 종료] → J2로 자동 배차되는지.

---

## 🔴 미해결

### 1. `align_with_rack`에서 `rack_area_id` 사용 불가
- regionType 미확인 — **AutoXing 문의 필요**.
- **[측정]** 2026-08-07 재확인: `BackEnd/app` · `frontend/app` · `frontend/lib` · `TabletApp/app/src` 전체에 `rack_area_id` 참조 **0건**.
- **[추정]** 앱 코드에 없으므로 SDK/펌웨어 쪽 제약으로 보이며, 우리 쪽 수정으로는 해결 불가.

### 2. `detectRackSize` REST API 없음
- SDK 전용 — **AutoXing 문의 필요**.
- **[측정]** 2026-08-07 재확인: `BackEnd/app` · `frontend`(node_modules 제외) 전체에 `detectRackSize`/`detect_rack_size` 참조 **0건**(대소문자 무시).

### 3. ~~`to_unload_point` J1 이동 미작동 (확인 필요)~~ → 🟢 **동작 확인됨 (2026-08-10)**
- **[측정] 2026-08-10 실기**: `to_unload_point` 이동(move 3627, 목표 `(3.1550, 3.0950)` = R1 좌표)이 **`succeeded`** 로 완료됐다. 이 타입이 안 된다는 근거는 없다 — 오히려 렉 반납에 **필요한** 타입이었다(아래 "신규 A" 참조).
- 이하 기존 조사 기록은 보존한다.
- **[측정]** 2026-08-07 재확인 — `to_unload_point` 사용처는 전부 레거시 경로:
  `jack_service.py:267`, `:840`, `:954`, `scheduler.py:173`, `robot.py:869`
  (`deadlock_monitor.py:129`는 주석). **`dispatch_service.py`에는 사용처 없음.**
- ⚠️ **정정(2026-08-07)**: 이전 문구 "VESA 배차는 `standard` 이동만 사용"은 **부정확**했다.
  `dispatch_service.py`가 실제로 쓰는 이동 타입은 **`align_with_rack`(:178), `standard`(:205, :256, :279), `charge`(:290)** 세 가지다.
  다만 `to_unload_point`를 쓰지 않는다는 **결론 자체는 유효** → 이 이슈의 VESA 영향 없음.
- **[측정] 2026-08-07 보강**: 렉 적재 상태에서 `standard`가 계속 `calculation_failed`로 실패하던 시간대에, `to_unload_point`(move id 3562)는 상태가 **`cancelled`**(= 계산 성공 후 이동하다 사용자 취소)였다. 즉 실패하지 않았다.
- **[추정]** `to_unload_point`는 근거리 정밀 이동이라 전역 경로 계산 단계를 거치지 않는 것으로 보임 → 아래 8번 진단의 보조 근거.

### 4. 맵 변경 시 경로 웨이포인트 POI ID 자동 매핑 필요
- 레거시 자동 모드용 — **VESA 영향 없음**. 레거시 재사용 시에만 필요.

### 5. [근본수정 필요] 맵 요소 저장이 POI를 delete+재생성 → 저장/동기화마다 POI id가 바뀜
- **[측정]** 2026-08-07 재확인: `crud/map.py`에 `db.query(MapPOI).filter(MapPOI.map_id == map_id).delete()`가 **두 곳**(`:291`, `:340`) 있고 이후 재INSERT. `models/map.py:75` `pois = relationship("MapPOI", ..., cascade="all, delete-orphan")` 확인.
  (이전 문구는 `:340` 한 곳만 적었으나 실제로는 2곳 — 수정 시 둘 다 봐야 함)
- 증상: 맵 저장 한 번에 POI id 전체 변경(예 J1 1015→1021) → **태블릿 슬롯 매핑·`robots.charging_id`/`standby_id`·경로 POI 참조가 전부 깨짐**. (2026-08-06 현장 세팅 중 "위치 정보를 찾을 수 없어요"(POI_NOT_FOUND) 원인)
- **[측정] 2026-08-10 — 파괴 메커니즘 규명**: 단순히 "id가 바뀐다"가 아니라 **DB 외래키가 참조를 자동으로 지운다.**
  ```
  FOREIGN KEY (charging_id) REFERENCES map_pois(id) ON DELETE SET NULL
  ```
  POI가 delete 되는 순간 이를 참조하던 `robots.charging_id` 가 **DB에 의해 NULL로 초기화**된다.
  → 8/7에는 자동 충전 복귀가 되던 Qs가 8/10에 `charging_id=NULL` 이었던 이유가 이것이다(그 사이 맵 저장).
- **[측정] 2026-08-10 실시간 재현 2건**
  1. 15:11 관제에서 POI 이름 하나(`W1`→`C1-1`)만 바꿔 저장 → map 26의 POI **6개 전부** id 변경(1057~1062 → 1063~1068), 좌표는 동일.
  2. 그 직후 `charging_id=1057` UPDATE 시도가 **FK 위반으로 거부**(1057이 이미 삭제됨). 태블릿 URL `/tablet/poi/1059` 도 무효화되어 호출 시 POI_NOT_FOUND 카드가 떴다.
  ⚠️ **맵을 새로 만들 때뿐 아니라, 기존 맵에서 이름 한 글자만 고쳐 저장해도 동일하게 깨진다.**
- 임시 대응: 맵 확정 후 저장/동기화 다시 누르지 않기 / 저장했으면 슬롯·charging_id·standby_id·태블릿 URL을 새 id로 재세팅(`GET /api/map/active-pois` 로 새 id 확인).
- 근본 해결: `save_map_elements`를 **delete+재생성 → name(또는 안정 키) 기준 upsert(id 유지)** 로 변경. 완료 시 저장해도 참조 안 깨짐.
  **우선순위 최상** — 2026-08-10 하루에만 2회 실제 피해. 9월말 besa 현장에서 맵 작업 시 매번 반복된다.

### 7. ~~충전 도킹 중에는 맵핑이 "조용히" 실패~~ → 🟢 **가드 적용 + 실기 확인 (2026-08-10)**
- **수정**: `robot_api/robot_live_service.py` 에 `is_charging(ip) -> bool | None` 신설(배터리 토픽만 최대 3초 수집해 `power_supply_status` 로 판정). `routers/map.py:api_create_mapping` 이 로봇에 POST를 보내기 **전에** 확인해 충전 중이면 **409 + "로봇이 충전독에 도킹된 상태에서는 맵이 생성되지 않습니다. 충전기에서 분리한 뒤 다시 시작하세요."** 를 반환.
- **설계 결정**: 판정 불가(오프라인·WS 실패·필드 없음)면 `None` 을 반환하고 **막지 않는다(fail-open)**. 오탐으로 정상 맵핑을 막는 쪽이 더 나쁘기 때문. `fetch_robot_live()` 는 REST 2회+WS로 최악 10초라 쓰지 않았다.
- **[측정]** `tests/test_is_charging.py` 16/16 통과. 2026-08-10 **실기 확인 완료**(사용자 확인).
- ⚠️ 근본 원인(펌웨어가 충전 중 SLAM을 안 돌리는 것)은 그대로다. 우리 쪽은 사전 차단 + 안내만 한다.
- 아래는 원인 조사 기록(보존).

#### (원 기록) 충전 도킹 중 맵핑 실패 조사
- 증상(2026-08-07 현장): 충전기에 도킹된 채 맵핑 시작 → 관제 모달이 **"맵 데이터 수신 대기 중"에서 영원히 안 넘어감**.
- 실측 근거: 로봇이 `POST /mappings`를 **201로 받아주고 `state: running`까지 만들지만** 실제 SLAM은 안 돌아감.
  - 도킹 중(세션 id 6, 5분 경과): `/map` = size `[0,0]`·data 0바이트, `/maps/5cm/1hz` 20초간 0건, `/trajectory` 0점. `/alerts`는 비어 있고 거부 코드도 안 옴.
  - 충전기 분리 후(세션 id 7): 수 초 만에 `/map` = `[470,236]`·9.9KB→12.5KB 증가, `/maps/5cm/1hz` 1Hz 정상, `/trajectory` 20→39점.
- **[측정]** **우리 코드에는 충전 상태 가드가 한 줄도 없음** — `map.py`/`robot_map_service.py`/`frontend/app/components/ui/map/` 전수 검색에서 맵핑 경로의 충전 관련 코드 0건.
- **[추정]** 따라서 차단 주체는 우리 코드가 아니라 로봇 펌웨어(axbot 2.12.21) 쪽으로 보임.
- 조치안: `api_create_mapping`에서 맵핑 POST 전에 충전 상태 확인(`robot_live_service.py:69-86`의 `power_supply_status` 판정 재사용) → 충전 중이면 로봇에 요청 보내지 않고 409 + "충전기에서 분리 후 다시 시도하세요" 반환. 프론트는 시작 즉시 카드 표시.
- ⚠️ 미확정: "충전 중"이 단독 원인인지, "정지 상태"도 영향이 있는지는 분리 검증 안 됨(분리 시점에 로봇을 물리적으로 옮겼음). **충전기 뗀 채 정지 상태로 맵핑 시작** 대조 실험 1회로 확정 가능.

### 8. 렉 적재 시 `calculation_failed` — 픽업 지점 클리어런스 부족 + RCS 무한 재시도
- 증상(2026-08-07 현장): 태블릿 "렉 포함" 호출 → 로봇이 렉까지 가서 `align_with_rack`·`jack_up`까지 성공한 뒤 **J1으로 출발하지 못하고 멈춤**. 화면엔 아무 안내 없음.
- **[측정]**
  - `standard` 이동이 `fail_reason 9 = calculation_failed - 道路不连通`("failed to calc global path")로 **73건 실패**(move id 3489~3565 구간, RCS가 약 6초 간격 재시도).
    ⚠️ **정정**: 이전 문구 "약 90건 **연속**"은 틀림. 실제 73건이고 **연속이 아니다** — 같은 구간에 비-실패 4건이 섞여 있음:
    `3554 standard/cancelled`, `3557 standard/succeeded`, `3558 align_with_rack/succeeded`, `3562 to_unload_point/cancelled`.
    특히 **`3557 standard/succeeded`(렉 없던 시점) → `3558 align_with_rack`(렉 정렬) → 3559부터 다시 전부 실패** 순서는 "렉을 든 뒤에만 실패"를 뒷받침한다.
  - 맵(`1SSS`, id 2) 점유격자 분석: 로봇→J1 직선 61개 지점 전부 free, J1 주변 0.5m 전부 free, **가상벽 feature 0개** → 정적 맵에 막힌 곳 없음.
  - **픽업 지점(`[3.1225, 3.15]`)의 최근접 장애물 거리 = 0.46 m** (맵 PNG + `grid_origin/resolution`으로 계산).
  - 로봇 footprint 최대 반경 **0.379m**, `rack.specs` S300 = `width/depth 0.765`, `margin 0.0925`.
  - 잭 다운 후 동일 목적지로 즉시 이동 성공(move 3568~3572 `succeeded`).
- **[추정]**
  - 렉 적재 시 유효 반경을 `0.765/2 + 0.0925 = 0.475m`로 보면 클리어런스 0.46m를 **약 15mm 초과** → 출발점이 무효 좌표가 되어 전역 경로 계산이 불가해진 것으로 보임.
    ※ 펌웨어가 실제로 이 식(폭/2 + margin)으로 팽창시키는지는 **문서/코드로 확인하지 못했다.** 확정하려면 픽업 위치를 여유 있는 곳으로 옮겨 재현되지 않는지 보는 게 가장 빠름.
  - 주변 점유 패턴이 벽이 아니라 흩어진 점 형태라, 과거 렉 다리 등이 맵에 장애물로 굳은 것으로 보임(맵 `1SSS`의 `create_time`은 2026-02월).
- **정정 기록**: `rack.specs`는 **정상 등록돼 있음** — `GET /system/settings/user`에 S300 spec 존재. `GET /robot-params`에는 안 나오므로 **그것만 보고 "미등록"으로 판단하지 말 것**(2026-08-07에 실제로 이 오판을 했음).
- 조치안(우선순위):
  1. **설정/배치로 해결 시도** — 랙 위치(standby) POI를 주변 여유 **0.6m 이상**인 곳으로 이동 후 재시험. 이것만으로 끝날 가능성 높음(코드 수정 불필요).
  2. 맵을 최신 맵핑본으로 교체(박제 장애물 제거). 단 5번 이슈(POI id 변경) 동반.
  3. **코드 수정 필요분** — RCS가 `calculation_failed`를 무한 재시도함. **재시도 횟수 제한** + 태블릿 실패 카드에 `calculation_failed` 문구 추가("렉을 실은 상태로는 지나갈 공간이 부족합니다").

### 9. ~~맵핑 모달 — `/maps/5cm/1hz` 폴백 무력화 + 무한 대기~~ → 🟢 **수정 + 실기 확인 (2026-08-10)**
- **수정**: `MappingModal.tsx`
  1. `/map` 토픽이 **실데이터(`data.length >= 10`)일 때만** `has5cmTopicRef` 를 세우도록 변경 → 빈 `/map` 때문에 `/maps/5cm/1hz` 폴백이 영구히 막히던 문제 해소.
  2. WS `msg.error` 를 `console.error` 에서 **화면 표시(`setError`)** 로 승격.
  3. `MAP_STALL_SEC = 15` — 15초간 맵 데이터가 한 장도 안 오면 안내("로봇이 충전독에 도킹돼 있으면 맵이 생성되지 않습니다…") + **[다시 시작] 버튼** 표시.
  4. 재시작 시 `has5cmTopicRef` 초기화.
- **[측정]** `npx tsc --noEmit` 통과(에러 0), eslint 에러 0. 2026-08-10 **실기 확인 완료**(사용자 확인).
- 아래는 원인 조사 기록(보존).

#### (원 기록) 맵핑 모달 폴백 무력화 조사
- **[측정]** `MappingModal.tsx:309-317`: `/map` 토픽이 **내용이 비어 있어도** 도착만 하면 `has5cmTopicRef = true`로 잠가 `/maps/5cm/1hz`를 영구 무시.
- **[측정]** 이 펌웨어는 맵핑 중(맵 미생성 상태) `/map`을 **빈 값(size `[0,0]`, data 0바이트)으로 계속 발행** — 20초 관측에서 22건 수신. 화면 전환 조건은 `data.length >= 10`(`:238`)이라 절대 충족되지 않음.
- **[추정]** 따라서 5cm 토픽이 정상 발행되는 상황에서도 화면에 안 그려질 것. (5cm가 실제로 오는데 무시되는 장면을 직접 관측하지는 못함 — 관측 당시엔 5cm도 발행되지 않았음)
- 수정안: `data.length >= 10`(실데이터)일 때만 플래그를 세움.
- 함께: 맵 데이터 15초 무수신 시 안내 + 재시도 버튼, WS `msg.error` 수신 시 화면 표시(현재 `console.error`만), 릴레이(`robot_map_service.py:317`)의 연결 종료 예외를 타임아웃과 구분해 큐에 push.

### 10. 로봇 하드웨어 경보(`/alerts`)가 관제에 전혀 안 뜸
- **[측정]** 백엔드·프론트 전체에서 `"/alerts"` 문자열 검색 **0건**(구독 코드 없음). `create_alarm_log` 호출처는 라우터 엔드포인트 1곳뿐이고 **백엔드 내부 호출 0건** — 알람 생성 경로는 프론트 `AlertContext.tsx:138`의 `createAlarmLog` 하나뿐.
- **[측정]** 2026-08-07 Qs(.101) `/alerts`에 `{code 1007, warning}`·`{code 9019, error, "Bottom sensor heartbeat error."}`가 실제로 떠 있었고, 같은 시각 Qr(.100)은 빈 배열. 이후 두 로봇 모두 빈 배열로 자연 해소됨.
- **[추정]** 위 두 사실을 합하면 로봇 하드웨어 경보는 MON-006 알림 내역에 남을 경로가 없으며, 실제로 rb-admin에서만 확인 가능했다(관제 화면에서 조회해 부재를 확인하지는 않음).
- 스토리보드 MON-006의 "오류 메시지·오류 코드·로봇 SN으로 검색" 의도와 어긋남.
- 조치안: 로봇 `/alerts` 주기 폴링(또는 WS 구독) → `alarm_logs` 적재. `stuck_state`/`fail_reason`도 함께 남기면 8번 같은 건의 사후 추적이 가능해짐.

---

## 🟡 완화 (우회 동작 중, 근본 미해결)

### 6. 잭 다운 후 로봇 빠져나오기 시간 불확실
- 현재: 고정 10초 대기 + 400 에러 시 5초 간격 재시도로 **동작은 함**. 근본적으로 소요시간이 상황따라 불확실.

---

## ⚠️ 에러 예상 이슈 (동시성·공정, 2026-08-06 코드 리뷰)

> 아직 "터진" 버그는 아니지만, **여러 대 로봇 + 여러 태블릿 동시 운영** 시 재현될 수 있는 지점들. 배차 엔드포인트는 동기 함수(def)라 요청마다 스레드풀에서 병렬 실행됨 → 아래 레이스가 실재.

### E1. 🔵 ~~[공정 갭] "모두 바쁠 때 먼저 끝난 로봇이 간다" 미구현~~ → **구현 완료(2026-08-10), 실기 검증 대기**
### E2. 🔵 ~~배정 경로에 락 없음 → 로봇 이중 배정~~ → **수정 완료(2026-08-10), 실기 검증 대기**
### E3. 🔵 ~~같은 POI 동시 호출 → 이중 예약~~ → **수정 완료(2026-08-10), 실기 검증 대기**
> 세 건은 하나의 수정으로 함께 해소했다. 상세는 문서 위쪽 **[E1 + E2/E3] 호출 대기열 + 배정 락** 항목 참조.
> 아래는 원래의 문제 분석 기록(보존).

#### (원 기록) E1. 공정 갭
- 현재: 모든 로봇이 작업 중이면 `call_to_poi`가 **거부**(`dispatch_service.py:706` `return False, "NO_ROBOT", None`) → 태블릿 "가용 로봇 없음". *(2026-08-07 재확인 — 이전 문구의 `:705`는 한 줄 밀린 값이었고, 반환값도 3-튜플로 바뀌어 있음)*
- 필요: 대기열(FIFO) 등록 → 워커 종료 시점(`_worker_loop` finally)에서 꺼내 자동 배정.
- 주의: 꺼낼 때 **락 필수**(두 워커 동시 종료 시 같은 호출 경쟁).

#### (원 기록) E2. 배정 경로에 락 없음 → 로봇 이중 배정
- `call_to_poi`가 `find_available_robot()`(`:580`)로 로봇 고른 뒤 `start_session()`(`:457`) 하는데 **"고르기→시작" 사이 락 없음**.
- `start_session`의 `has_active_worker` 체크(`:462`)와 `_set_worker` 사이 틈으로 두 스레드 동시 통과 가능 → **로봇 1대에 워커 2개** → 한 로봇에 상반된 이동명령. (`_workers_lock`은 dict 개별 접근만 보호, 복합동작은 비원자적)
- 재현 조건: 여러 태블릿이 거의 동시에 호출.
- **재현 검증(2026-08-06)**: 실제 배정 로직 구조(체크 → DB지연 → 세팅)를 격리 모델로 재현 → **8스레드 동시 배정 200회 중 200회 전부 이중배정**(최악 로봇1대에 워커 8개). 배정을 단일 락으로 원자화한 수정본은 **0/200회**. 스크립트: `tests/race_repro.py` (라이브 DB/로봇 미접촉, 순수 로직 재현). **E3도 동일한 check-then-act 패턴이라 같은 결함.**
  - 한계: 라이브 서버의 실제 발생률은 타이밍/DB지연에 따라 다름. "한 명씩 순차 호출 → 거의 안 남 / 여러 명 동시 호출 → 남".

#### (원 기록) E3. 같은 POI 동시 호출 → 이중 예약
- 점유 검증(`get_session_at_poi`/`get_session_heading_to_poi`)이 **check-then-act**라, 둘 다 "세션 없음"으로 통과 → 로봇 2대가 같은 POI로 이동.
- 태블릿 UI의 점유 비활성화는 화면 상태일 뿐, 백엔드 최종검증이 racy.

### E4. 🟡 로봇 실제 상태 ↔ 백엔드 세션 불일치
- 배차·가용 판단이 DB 세션 + 메모리 `_workers`로만 돌아감. rb-admin/강제제어(dock 등)로 수동 조작하면 **세션이 안 닫혀 꼬임**(2026-08-06 "가용 1대" 원인).
- 필요: 로봇을 주기 폴링해 실제 위치/도킹과 세션을 **화해(reconcile)** → 수동 도킹 감지 시 세션 자동 종료.

### E5. 🟡 재시작 복구 유령 세션
- **[측정]** `dispatch_service.py:537 def recover_on_startup()`, 기동 시 `main.py:64`에서 호출. 미완료 세션을 `awaiting_next`로 되살림(`:361` 등).
- **[추정]** 되살린 세션의 워커가 로봇을 붙잡으므로, 로봇이 실제로 거기 없으면 **가용 0** 유령이 생김. (실제 유령 발생 장면을 재현 검증하지는 않음)

### E6. 🟡 zone 대기 60초 후 강제 진행
- **[측정]** `zone_guard.py:27 WAIT_FOR_ZONE_TIMEOUT_SEC = 60.0`, `:110`에서 타임아웃 시 `"대기 타임아웃 — 강제 진행"` 로그와 함께 진행.
- **[추정]** 좁은 통로에 로봇이 밀집하면 충돌 위험. (충돌이 실제로 난 사례는 없음)

### E7. 🟡 프론트(관제) WS 끊김 미반영
- WebSocket이 죽어도 화면은 "연결됨"으로 남음. 자동 재연결/heartbeat 없음. (2026-08-05 확인)

### E8. 🟡 TabletApp 네트워크 순단 처리 없음
- **[측정]** `TabletApp/.../MainActivity.kt:41 cacheMode = WebSettings.LOAD_NO_CACHE`. 화면은 `:61 webView.loadUrl("${serverUrl}/api/dispatch/tablet/$slotNumber")` 한 번 로드가 전부.
- **[추정]** 캐시를 안 쓰고 재로드 로직도 없으므로 순단 시 흰 화면으로 멈출 것. 키오스크 안정성 위해 재시도+자동 새로고침 필요. (실제 순단 재현 테스트는 안 함)
- 슬롯 기반이라 POI id 바뀌면 슬롯 매핑 깨짐(위 5번 이슈와 연결).

### E9. 🟡 태블릿 "출발" 표시가 백엔드 잭 시간에 하드코딩
- **[측정]** 2026-08-07 재확인: `dispatch_tablet.html:860` `setTimeout(..., 8000)` + 주석 "8초간 '준비 중' 표시 → 그 후 ✓ '출발' 메시지로 전환 (백엔드 JACK_SETTLE_SEC와 동일)". 백엔드는 `dispatch_service.py:34` `JACK_SETTLE_SEC = 8`(사용처 `:197`, `:225`, `:235`).
- **[추정]** 실제 잭 동작이 8초보다 오래 걸리면 화면이 먼저 "출발"로 넘어감. 백엔드 상태 기반으로 전환 권장.

### 개선 우선순위(2026-08-10 갱신)
1. ~~**E1+E2 함께** — 대기열 + 배정 원자화(E3도 해소)~~ → **완료(2026-08-10), 실기 검증만 남음**
2. **5번 — POI upsert** ← **현재 최우선.** 오늘 하루에만 2회 실제 피해, 9월말 besa 현장에서 맵 작업 시 매번 반복된다.
3. **E4** — 상태 화해 루프. (rb-admin 수동 조작과 세션 불일치)
4. **10번** — 로봇 경보 관제 표시. *2026-08-10 사용자 판단으로 보류.*
5. 나머지(E5~E9)는 운영 안정성/UX 개선.

### 검증 대기 목록 (실기 테스트 시 확인할 것)
| 항목 | 확인 방법 |
|---|---|
| 8번 조치안 3 | 렉을 주변 여유 0.5m 미만인 자리에 두고 "렉 포함" 호출 → ~20초 내 "공간이 부족" 카드 |
| E1 대기열 | 로봇 1대만 켜고 J1 호출(배정) → J2 호출("대기열 1번째") → J1 [종료] → J2로 자동 배차 |
| E2/E3 락 | 두 화면에서 같은 POI를 동시에 눌러 로봇이 1대만 가는지 |
| 7번 대조 실험 | 충전기 뗀 채 **정지 상태**로 맵핑 시작 → "충전 중"이 단독 원인인지 확정 |

---

## 🟢 해결완료 (실기 확인)

### [신규 A] 렉 반납이 R1 정위치가 아니라 그 앞에서 이뤄짐 — 2026-08-10 발견·수정·확인
- 증상: [작업 종료] 시 렉을 R1이 아니라 **R1으로 들어가기 직전 지점**에 내려놓고 대기.
- **[측정]** `move 3621`(`standard`)은 목표 `(3.155, 3.095)` = R1 좌표로 정확히 나갔고 응답도 `succeeded` 였는데, 로봇 실제 위치는 `(3.205, 2.380)` — **R1에서 0.717m 앞**이었다. `standard` 는 목표 근처 도달로 성공 처리되며 렉을 제자리에 놓는 정밀 정렬을 하지 않는다.
- **[측정]** 레거시 경로(`jack_service.py` 의 `force_return`)는 같은 동작에 **`to_unload_point`** 를 쓰고 있었다(`max_attempts=30, timeout=120`). VESA `dispatch_service` 만 `standard` 였다.
- **수정**: `_return_to_standby_and_park` 의 standby 복귀 이동을 `to_unload_point` 로 변경 + 실패 시 `standard` 폴백(이슈 3번이 당시 미확정이었기에 안전장치로 둠 — 최소한 이전 수준은 보장).
- **[측정] 검증(2026-08-10)**: `move 3627` = `to_unload_point`, 목표 `(3.1550, 3.0950)`, `succeeded`. 육안으로도 정위치 반납 확인(사용자).
  ⚠️ 잭다운 시점의 로봇 좌표는 측정하지 않았다 — 물리적 정확도의 근거는 육안 확인뿐이다.

### [신규 B] 충전소 자동 복귀 + `C1-1` 경유 동작 — 2026-08-10 확인
- 증상: [작업 종료]를 눌러도 로봇이 그 자리에 서 있고 충전소로 복귀하지 않음.
- **[측정] 원인**: `robots.charging_id` 가 NULL 이면 `_load_charging_poi()` 가 None 을 돌려주고, `_return_to_standby_and_park` 의 **충전소 복귀 블록 전체를 건너뛴다**(경유지 `C1-1` 조회도 그 안에 있어 함께 스킵된다).
- **[측정] 근거**: 8/10 낮의 `charge` 이동은 전부 `creator: robot-admin-web`(수동) 또는 `control_unit`(로봇 자체)이었고 **`creator: rcs` 는 0건**이었다. 반면 8/7에는 `rcs` charge 2건(3569·3594)이 성공했고, 이는 세션 62·63(둘 다 Qs)과 시각이 일치한다. → 8/7엔 `charging_id` 가 있었고 그 뒤 NULL 이 됐다(원인은 5번 이슈의 FK `ON DELETE SET NULL`).
- **조치**: `robots.charging_id` 를 map 26의 `C1`(id 1063)으로 설정. 경유지는 기존 `W1` POI를 **`C1-1`** 로 이름 변경(코드가 `f"{charger_name}-1"` 로 정확히 찾기 때문). 위치는 이미 적합했다 — C1까지 **1.148m**(권장 1.0~1.5m), 충전소 바라보는 방향 오차 **9.2°**.
- **[측정] 검증(2026-08-10)**: 세션 73이 `completed`. 이동 기록 전부 `creator: rcs` · `fail_reason: 0` · `succeeded` —
  `3627 to_unload_point (R1)` → `3628 standard (-0.2100, 0.8975)` = **C1-1 좌표와 일치** → `3629 charge (0.0636, -0.2175)` = **C1 좌표와 일치**.
  최종 로봇 위치 `(0.055, -0.323)`(C1에서 0.106m), `is_charging()` = **True**.
- ⚠️ 이 세팅(`charging_id=1063`)은 **맵을 다시 저장하면 FK에 의해 NULL로 날아간다**(5번 이슈). 저장 후엔 재설정 필요.

### [문서 정정] 로봇 맵 동기화 판정 기준 — `create_time` 이 아니라 `last_modified_time`
- 2026-08-10에 로봇 `.101`의 `current-map` 이 `1SSS`, `create_time` 2026-02-10 으로 보여 "옛날 맵을 쓰는 중"으로 **오판**했다.
- **[측정]** 실제로는 DB map 26 = 로봇 맵 id 2(`1SSS`) 로 동일했다. 근거 4가지가 모두 일치:
  `robot_maps.robot_map_id = 2` / `robot_sn = Qs` / grid_origin `-27.85, -7.45` 동일 / **맵 이미지 md5 완전 일치**(23500 bytes, `d5fd76c28132…`). POI 좌표 4개도 전부 일치.
- 원인: 로봇의 기존 맵 슬롯에 **덮어쓰기**로 동기화하면 `create_time` 은 그 슬롯이 처음 생성된 시각으로 남는다. 갱신되는 건 **`last_modified_time`**(당시 값 = 2026-08-10 13:09:12, 동기화 시각과 일치)과 `map_version`(=2).
- → `현장세팅_체크리스트.md` 7-2 수정 완료.
